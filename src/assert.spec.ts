/**
 * assert.ts is only a wrapper around check.ts and util.ts, so no test is needed.
 * It's just a way to throw an error if the check fails.
 */

describe('assert', () => {
  it('No test is needed', () => {});
});
