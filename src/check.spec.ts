/**
 * check.ts only contains native function calls, so no test is needed.
 * Basically, it's a wrapper around typeof and also check for null (value).
 */

describe('check', () => {
  it('No test is needed', () => {});
});
